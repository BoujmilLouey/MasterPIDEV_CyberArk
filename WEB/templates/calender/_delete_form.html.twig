<form method="post" action="{{ path('calender_delete', {'id': calender.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ calender.id) }}">
    <button class="btn">Delete</button>
</form>
